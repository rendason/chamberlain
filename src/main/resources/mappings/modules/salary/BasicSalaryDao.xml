<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.thinkgem.jeesite.modules.salary.dao.BasicSalaryDao">
    
	<sql id="basicSalaryColumns">
		a.id AS "id",
		a.user_id AS "user.id",
		a.item_name AS "itemName",
		a.amount AS "amount",
		u2.name AS "user.name"
	</sql>
	
	<sql id="basicSalaryJoins">
		LEFT JOIN sys_user u2 ON u2.id = a.user_id
	</sql>
    
	<select id="get" resultType="BasicSalary">
		SELECT 
			<include refid="basicSalaryColumns"/>
		FROM cl_basic_salary a
		<include refid="basicSalaryJoins"/>
		WHERE a.id = #{id}
	</select>
	
	<select id="findList" resultType="BasicSalary">
		SELECT 
			<include refid="basicSalaryColumns"/>
		FROM cl_basic_salary a
		<include refid="basicSalaryJoins"/>
		<where>
			
			<if test="user != null and user.id != null and user.id != ''">
				AND a.user_id = #{user.id}
			</if>
		</where>
		<choose>
			<when test="page !=null and page.orderBy != null and page.orderBy != ''">
				ORDER BY ${page.orderBy}
			</when>
			<otherwise>
			</otherwise>
		</choose>
	</select>
	
	<select id="findAllList" resultType="BasicSalary">
		SELECT 
			<include refid="basicSalaryColumns"/>
		FROM cl_basic_salary a
		<include refid="basicSalaryJoins"/>
		<where>
			
		</where>		
		<choose>
			<when test="page !=null and page.orderBy != null and page.orderBy != ''">
				ORDER BY ${page.orderBy}
			</when>
			<otherwise>
			</otherwise>
		</choose>
	</select>
	
	<insert id="insert">
		INSERT INTO cl_basic_salary(
			id,
			user_id,
			item_name,
			amount
		) VALUES (
			#{id},
			#{user.id},
			#{itemName},
			#{amount}
		)
	</insert>
	
	<update id="update">
		UPDATE cl_basic_salary SET 	
			user_id = #{user.id},
			item_name = #{itemName},
			amount = #{amount}
		WHERE id = #{id}
	</update>
	
	<update id="delete">
		DELETE FROM cl_basic_salary
		WHERE id = #{id}
	</update>
	
</mapper>