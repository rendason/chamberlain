<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.thinkgem.jeesite.modules.trade.dao.PurchaseDao">
    
	<sql id="purchaseColumns">
		a.id AS "id",
		a.name AS "name",
		a.quantity AS "quantity",
		a.price AS "price",
		a.unit AS "unit",
		a.saler AS "saler",
		a.consumer AS "consumer.id",
		a.remark AS "remark",
		u7.name AS "consumer.name"
	</sql>
	
	<sql id="purchaseJoins">
		LEFT JOIN sys_user u7 ON u7.id = a.consumer
	</sql>
    
	<select id="get" resultType="Purchase">
		SELECT 
			<include refid="purchaseColumns"/>
		FROM cl_purchase a
		<include refid="purchaseJoins"/>
		WHERE a.id = #{id}
	</select>
	
	<select id="findList" resultType="Purchase">
		SELECT 
			<include refid="purchaseColumns"/>
		FROM cl_purchase a
		<include refid="purchaseJoins"/>
		<where>
			
			<if test="name != null and name != ''">
				AND a.name LIKE 
					<if test="dbName == 'oracle'">'%'||#{name}||'%'</if>
					<if test="dbName == 'mssql'">'%'+#{name}+'%'</if>
					<if test="dbName == 'mysql'">concat('%',#{name},'%')</if>
			</if>
			<if test="saler != null and saler != ''">
				AND a.saler LIKE 
					<if test="dbName == 'oracle'">'%'||#{saler}||'%'</if>
					<if test="dbName == 'mssql'">'%'+#{saler}+'%'</if>
					<if test="dbName == 'mysql'">concat('%',#{saler},'%')</if>
			</if>
			<if test="consumer != null and consumer.id != null and consumer.id != ''">
				AND a.consumer = #{consumer.id}
			</if>
		</where>
		<choose>
			<when test="page !=null and page.orderBy != null and page.orderBy != ''">
				ORDER BY ${page.orderBy}
			</when>
			<otherwise>
			</otherwise>
		</choose>
	</select>
	
	<select id="findAllList" resultType="Purchase">
		SELECT 
			<include refid="purchaseColumns"/>
		FROM cl_purchase a
		<include refid="purchaseJoins"/>
		<where>
			
		</where>		
		<choose>
			<when test="page !=null and page.orderBy != null and page.orderBy != ''">
				ORDER BY ${page.orderBy}
			</when>
			<otherwise>
			</otherwise>
		</choose>
	</select>
	
	<insert id="insert">
		INSERT INTO cl_purchase(
			id,
			name,
			quantity,
			price,
			unit,
			saler,
			consumer,
			remark
		) VALUES (
			#{id},
			#{name},
			#{quantity},
			#{price},
			#{unit},
			#{saler},
			#{consumer.id},
			#{remark}
		)
	</insert>
	
	<update id="update">
		UPDATE cl_purchase SET 	
			name = #{name},
			quantity = #{quantity},
			price = #{price},
			unit = #{unit},
			saler = #{saler},
			remark = #{remark}
		WHERE id = #{id}
	</update>
	
	<update id="delete">
		DELETE FROM cl_purchase
		WHERE id = #{id}
	</update>
	
</mapper>