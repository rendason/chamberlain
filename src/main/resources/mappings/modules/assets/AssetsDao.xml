<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.thinkgem.jeesite.modules.assets.dao.AssetsDao">
    
	<sql id="assetsColumns">
		a.id AS "id",
		a.name AS "name",
		a.type AS "type",
		a.category AS "category",
		a.quantity AS "quantity",
		a.price AS "price",
		a.unit AS "unit",
		a.remark AS "remark"
	</sql>
	
	<sql id="assetsJoins">
	</sql>
    
	<select id="get" resultType="Assets">
		SELECT 
			<include refid="assetsColumns"/>
		FROM cl_assets a
		<include refid="assetsJoins"/>
		WHERE a.id = #{id}
	</select>
	
	<select id="findList" resultType="Assets">
		SELECT 
			<include refid="assetsColumns"/>
		FROM cl_assets a
		<include refid="assetsJoins"/>
		<where>
			
			<if test="name != null and name != ''">
				AND a.name LIKE 
					<if test="dbName == 'oracle'">'%'||#{name}||'%'</if>
					<if test="dbName == 'mssql'">'%'+#{name}+'%'</if>
					<if test="dbName == 'mysql'">concat('%',#{name},'%')</if>
			</if>
			<if test="category != null and category != ''">
				AND a.category = #{category}
			</if>
		</where>
		<choose>
			<when test="page !=null and page.orderBy != null and page.orderBy != ''">
				ORDER BY ${page.orderBy}
			</when>
			<otherwise>
			</otherwise>
		</choose>
	</select>
	
	<select id="findAllList" resultType="Assets">
		SELECT 
			<include refid="assetsColumns"/>
		FROM cl_assets a
		<include refid="assetsJoins"/>
		<where>
			
		</where>		
		<choose>
			<when test="page !=null and page.orderBy != null and page.orderBy != ''">
				ORDER BY ${page.orderBy}
			</when>
			<otherwise>
			</otherwise>
		</choose>
	</select>
	
	<insert id="insert">
		INSERT INTO cl_assets(
			id,
			name,
			type,
			category,
			quantity,
			price,
			unit,
			remark
		) VALUES (
			#{id},
			#{name},
			#{type},
			#{category},
			#{quantity},
			#{price},
			#{unit},
			#{remark}
		)
	</insert>
	
	<update id="update">
		UPDATE cl_assets SET 	
			name = #{name},
			type = #{type},
			category = #{category},
			quantity = #{quantity},
			price = #{price},
			unit = #{unit},
			remark = #{remark}
		WHERE id = #{id}
	</update>
	
	<update id="delete">
		DELETE FROM cl_assets
		WHERE id = #{id}
	</update>
	
</mapper>